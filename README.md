# lares

Household agents on your own server. Engine repo — see `CLAUDE.md` for the rules and `docs/` for the corpus (in progress).

Lares contains the shared engine. Installation settings, credentials and personal data belong outside this repository.

Agent definition Git backups are optional and off by default. See [definition backups](docs/runbooks/definition-backups.md) for what is saved, privacy and setup.

## Development and attribution

Lares is developed with substantial code contributions generated by Anthropic Claude and OpenAI Codex, under the maintainer's direction. The maintainer is responsible for deciding what is reviewed, merged and released. GitHub's contributor list reflects commit attribution and does not fully describe the tools used to write the code.

## Upgrading

See [the naming migration](docs/runbooks/naming-migration.md) before upgrading an existing installation to the Lares package namespace and local state paths.

## Status and development

Lares is an experimental alpha under active development. It is not a promise of production readiness or a support SLA. Run an evaluation on a disposable environment before connecting real accounts. Optional integrations require their own configuration and provider API credentials; consumer chat subscriptions do not supply API access.

Use Node.js 24 and pnpm 9.15.0. From the repository root, run `pnpm install --frozen-lockfile`, then `pnpm typecheck`. The test suites use disposable PostgreSQL containers, so Docker must be running. See [PR CI coverage and exclusions](docs/pr-ci.md) before interpreting a green result. Image builds and runtime probes run in CI; build and deployment are separate operations.

The remaining setup limitations include legacy integration instance bindings and an unproved
fresh-install conversation path. See the migration runbook before upgrading.

## License and contributions

Original Lares code is licensed under the GNU Affero General Public License, version 3 only ([LICENSE](LICENSE)), except where a file states otherwise. Third-party components retain their own licences and notices; see [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md).

See [CONTRIBUTING.md](CONTRIBUTING.md) for contribution requirements and [SECURITY.md](SECURITY.md) for reporting vulnerabilities.
