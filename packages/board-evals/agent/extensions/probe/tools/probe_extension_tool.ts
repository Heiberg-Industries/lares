// D10's sentinel: the mount switches the extension's own copy off, leaving the name free for the
// agent's dynamic catalogue to supply (Q3).
import { disableTool } from "eve/tools";

export default disableTool();
